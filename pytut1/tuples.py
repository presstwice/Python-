coordinates = (4, 5)
print(coordinates[0])

#tuples are containers where you can store different types of values
#tuples are created using = () while lists are created using []
#tuples are immutable so they cannot be changed
#you can put tuples in lists